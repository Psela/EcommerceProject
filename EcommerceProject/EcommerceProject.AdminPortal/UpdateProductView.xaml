<Page x:Class="EcommerceProject.AdminPortal.UpdateProductView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="UpdateProductView">

    <Grid Background="White">

        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="Trebuchet MS Bold" FontSize="15" Foreground="Gray"> Update Product</TextBlock>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="1" Grid.Column="0"  FontFamily="Trebuchet MS " VerticalAlignment="Center" HorizontalAlignment="Right" Height="30" Margin="20" Padding="8">Enter Product ID:</TextBlock>
            <TextBox Name="itemToUpdate"  Grid.Row="1" Grid.Column="1" HorizontalAlignment="Left" Width="120" Height="25" Padding="3" Margin="20"/>
        </Grid>
        <Button Grid.Row="2" Command="{Binding Update}" Height="30" Width="150" Content="Submit" Background="#64615C" Foreground="#f2f2f2"/>
    </Grid>
</Page>
